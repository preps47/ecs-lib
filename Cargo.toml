[workspace]
members = [
    "ecs-derive"
]

resolver = "3"

[package]
name = "ecs-lib"
version = "0.1.0"
edition = "2024"

[dependencies]
ecs-derive =  { path = "ecs-derive", optional = true }

[features]
default = ["derive"]
derive = ["dep:ecs-derive"]